#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    cout << (a < b) << " ";
    cout << (c > a) << " ";
    cout << (a == b) << " ";
    cout << (a != c) << " ";
    cout << (c <= b);

    return 0;
}
