#include <iostream>
using namespace std;

int main() {

    int a, b;

    cin >> a >> b;

    if(a == b)
        cout << "Empate";

    else if(a == 1 && b == 3)
        cout << "Jugador 1";

    else if(a == 2 && b == 1)
        cout << "Jugador 1";

    else if(a == 3 && b == 2)
        cout << "Jugador 1";

    else
        cout << "Jugador 2";

    return 0;
}
